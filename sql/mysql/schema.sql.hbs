---
basePath: "sql"
filename: "{{table}}.sql"
overwrite: never
---
-- Generated by crud-cli at {{datetime}}
-- Model: {{model_pascal}}  Table: {{table}}
-- Author: {{user_name}} <{{user_email}}>

CREATE TABLE IF NOT EXISTS `{{table}}` (
{{#each fields}}
  `{{name_snake}}` {{ty_map type}}{{#if length}}({{length}}){{/if}}{{#if is_pk}} NOT NULL{{#if @first}} AUTO_INCREMENT{{else}} PRIMARY KEY{{/if}}{{#unless nullable}} NOT NULL{{else}} NULL{{/unless}}{{/if}}{{#if unique}} UNIQUE{{/if}}{{#if default}} DEFAULT {{default}}{{/if}}{{#if comment}} COMMENT '{{comment}}'{{/if}}{{#unless @last}},{{/unless}}
{{/each}}
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci{{#if table_comment}} COMMENT='{{table_comment}}'{{/if}};
