---
basePath: "java/{{package_path}}/dto"
filename: "{{model_pascal}}UpdateDTO.java"
---
package {{package}}.dto;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;

/**
 * <p>
 * {{table_comment}} 修改 DTO
 * </p>
 *
 * @author {{user_name}}
 * @since {{datetime}}
 */
@Data
@Schema(description = "{{table_comment}}修改DTO")
public class {{model_pascal}}UpdateDTO {

{{#each fields}}
{{#if is_pk}}
    @NotNull(message = "ID不能为空")
    @Schema(description = "{{comment}}", requiredMode = Schema.RequiredMode.REQUIRED)
    private {{ty_map type}} {{name_camel}};

{{else}}
{{#unless skip_update}}
{{#if is_required}}
    @NotNull(message = "{{comment}}不能为空")
{{/if}}
{{#if length}}
    @Size(max = {{length}}, message = "{{comment}}长度不能超过{{length}}个字符")
{{/if}}
    @Schema(description = "{{comment}}")
    private {{ty_map type}} {{name_camel}};

{{/unless}}
{{/if}}
{{/each}}
}
