---
basePath: "java/{{package_path}}/pojo/dto"
filename: "{{model_pascal}}CreateDTO.java"
---
package {{package}}.{{domain}}.pojo.dto;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import lombok.experimental.Accessors;

/**
 * <p>
 * {{model_pascal}} 添加DTO
 * </p>
 *
 * @author {{git_user_name}}
 * @since {{datetime}}
 */
@Data
@Accessors(chain = true)
@Schema(description = "{{model_pascal}}添加DTO")
public class {{model_pascal}}CreateDTO {

{{#each fields}}
{{#unless is_pk}}
    @Schema(description = "{{name}}")
    private {{type}} {{name_camel}};

{{/unless}}
{{/each}}
}
